import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useMyCultureContributions } from '@/features/culture/culture.hooks';
export default function CultureContributionsScreen(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const items=useMyCultureContributions();return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 text-2xl font-extrabold" style={{color:colors.text}}>Mes contributions</Text></View>{items.isLoading?<View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary}/></View>:items.isError?<State text="Vos contributions sont indisponibles." retry={()=>items.refetch()}/>:<FlatList data={items.data?.content??[]} keyExtractor={(item)=>item.id} contentContainerStyle={{padding:16}} renderItem={({item})=><TouchableOpacity onPress={()=>router.push(`/(explore)/culture/${item.id}`)} className="mb-3 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><Text className="font-bold" style={{color:colors.text}}>{item.slug.replace(/-/g,' ')}</Text><Text className="mt-1 text-xs font-semibold text-[#EF4444]">{item.status.replace(/_/g,' ')} · {item.verificationStatus}</Text><Text className="mt-2 text-sm" style={{color:colors.textSecondary}}>{item.countryCode} · {item.primaryLanguageCode.toUpperCase()}</Text></TouchableOpacity>} ListEmptyComponent={<State text="Vous n’avez encore envoyé aucune contribution."/>}/>}</SafeScreen>;}
function State({text,retry}:{text:string;retry?:()=>void}){const colors=useThemeStore((s)=>s.colors);return <View className="flex-1 items-center justify-center px-8"><Icon name="leaf-outline" size={42} color={colors.textMuted}/><Text className="mt-4 text-center" style={{color:colors.textSecondary}}>{text}</Text>{retry?<TouchableOpacity onPress={retry} className="mt-4"><Text className="font-bold text-[#EF4444]">Réessayer</Text></TouchableOpacity>:null}</View>;}
