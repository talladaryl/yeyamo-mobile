import { Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useAuthStore } from '@/features/auth/auth.store'; import { useArtwork } from '@/features/artworks/artworks.hooks';
export default function SavedArtworksScreen(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const demo=useAuthStore((s)=>s.sessionMode?.startsWith('demo-')??false);const sample=useArtwork(demo?'demo-artwork-1':undefined);return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 text-2xl font-extrabold" style={{color:colors.text}}>Œuvres enregistrées</Text></View>{demo&&sample.data?<TouchableOpacity onPress={()=>router.push('/(explore)/artworks/demo-artwork-1')} className="mx-4 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><Text className="font-bold" style={{color:colors.text}}>{sample.data.artwork.title}</Text><Text className="mt-1 text-sm" style={{color:colors.textSecondary}}>Compte démo · collection locale</Text></TouchableOpacity>:<View className="flex-1 items-center justify-center px-8"><Icon name="bookmark-outline" size={42} color={colors.textMuted}/><Text className="mt-4 text-center" style={{color:colors.textSecondary}}>La synchronisation des œuvres enregistrées sera activée avec l’endpoint de favoris. Aucun contenu fictif n’est affiché.</Text></View>}</SafeScreen>;}
