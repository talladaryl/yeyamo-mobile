import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useMyCultureChallenges } from '@/features/culture/culture.hooks';
export default function CultureChallengesScreen(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const mine=useMyCultureChallenges();return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 text-2xl font-extrabold" style={{color:colors.text}}>Mes défis</Text></View>{mine.isLoading?<View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary}/></View>:mine.isError?<State text="Vos défis sont indisponibles." retry={()=>mine.refetch()}/>:<FlatList data={mine.data??[]} keyExtractor={(item)=>item.challengeId} contentContainerStyle={{padding:16}} renderItem={({item})=><TouchableOpacity onPress={()=>router.push(`/(explore)/challenges/${item.challengeId}`)} className="mb-3 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><Text className="font-bold" style={{color:colors.text}}>Défi {item.challengeId}</Text><Text className="mt-1 text-sm" style={{color:colors.textSecondary}}>Participation le {new Date(item.joinedAt).toLocaleDateString('fr-FR')}</Text><Text className="mt-2 text-xs font-bold text-[#7C3AED]">{item.completedAt?'Terminé':'En cours'}</Text></TouchableOpacity>} ListEmptyComponent={<State text="Vous ne participez encore à aucun défi."/>}/>}</SafeScreen>;}
function State({text,retry}:{text:string;retry?:()=>void}){const colors=useThemeStore((s)=>s.colors);return <View className="flex-1 items-center justify-center px-8"><Icon name="trophy-outline" size={42} color={colors.textMuted}/><Text className="mt-4 text-center" style={{color:colors.textSecondary}}>{text}</Text>{retry?<TouchableOpacity onPress={retry} className="mt-4"><Text className="font-bold text-[#EF4444]">Réessayer</Text></TouchableOpacity>:null}</View>;}
