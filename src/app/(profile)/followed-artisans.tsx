import { Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useAuthStore } from '@/features/auth/auth.store'; import { ArtisanCard } from '@/features/artisans/components/ArtisanCard'; import { useArtisan } from '@/features/artisans/artisans.hooks';
export default function FollowedArtisansScreen(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const demo=useAuthStore((s)=>s.sessionMode?.startsWith('demo-')??false);const artisan=useArtisan(demo?'demo-artisan-1':undefined);return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 text-2xl font-extrabold" style={{color:colors.text}}>Artisans suivis</Text></View>{demo&&artisan.data?<ArtisanCard artisan={artisan.data} onPress={()=>router.push('/(explore)/artisans/demo-artisan-1')}/>:<View className="flex-1 items-center justify-center px-8"><Icon name="people-outline" size={42} color={colors.textMuted}/><Text className="mt-4 text-center" style={{color:colors.textSecondary}}>Le suivi artisan utilisera le graphe social existant dès que les cibles artisan seront exposées. Aucun faux suivi n’est affiché.</Text></View>}</SafeScreen>;}
