import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useLanguageProgress } from '@/features/culture/culture.hooks';
export default function LanguageProgressScreen(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const progress=useLanguageProgress();return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 text-2xl font-extrabold" style={{color:colors.text}}>Progression linguistique</Text></View>{progress.isLoading?<View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary}/></View>:progress.isError?<State text="La progression n’est pas disponible." retry={()=>progress.refetch()}/>:<FlatList data={progress.data??[]} keyExtractor={(item)=>item.id} contentContainerStyle={{padding:16}} renderItem={({item})=><View className="mb-3 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><Text className="text-lg font-bold" style={{color:colors.text}}>{item.languageCode.toUpperCase()}</Text><Text className="mt-1 text-sm" style={{color:colors.textSecondary}}>{item.completedAt?'Leçon terminée':'Leçon commencée'} · {item.attemptCount} tentative(s)</Text>{item.score!==null?<Text className="mt-2 font-bold text-[#EF4444]">Score {item.score}%</Text>:null}</View>} ListEmptyComponent={<State text="Aucune progression enregistrée."/>}/>}</SafeScreen>;}
function State({text,retry}:{text:string;retry?:()=>void}){const colors=useThemeStore((s)=>s.colors);return <View className="flex-1 items-center justify-center px-8"><Icon name="language-outline" size={42} color={colors.textMuted}/><Text className="mt-4 text-center" style={{color:colors.textSecondary}}>{text}</Text>{retry?<TouchableOpacity onPress={retry} className="mt-4"><Text className="font-bold text-[#EF4444]">Réessayer</Text></TouchableOpacity>:null}</View>;}
