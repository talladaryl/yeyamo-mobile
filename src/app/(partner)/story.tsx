import CreateStoryScreen from '@/app/(create)/story';

/**
 * Partners use the same verified Story creation contract. Promotional stories
 * stay unavailable until the backend exposes a campaign/resource association.
 */
export default CreateStoryScreen;
