import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useArtisanOrders } from '@/features/artwork-orders/artwork-orders.hooks'; import { formatMoney } from '@/utils/format';
export default function PartnerArtworkOrders(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const orders=useArtisanOrders();return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 text-2xl font-extrabold" style={{color:colors.text}}>Commandes d’œuvres</Text></View>{orders.isLoading?<View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary}/></View>:orders.isError?<View className="flex-1 items-center justify-center"><Text style={{color:colors.textSecondary}}>Les commandes sont indisponibles.</Text></View>:<FlatList data={orders.data??[]} keyExtractor={(item)=>item.id} contentContainerStyle={{padding:16}} renderItem={({item})=><TouchableOpacity onPress={()=>router.push(`/(partner-dashboard)/artwork-orders/${item.id}`)} className="mb-3 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><View className="flex-row justify-between"><Text className="font-bold" style={{color:colors.text}}>{item.reference}</Text><Text className="text-xs font-bold text-[#EF4444]">{item.status}</Text></View><Text className="mt-2 text-sm" style={{color:colors.textSecondary}}>{formatMoney(item.grossAmount,item.currencyCode)} · {item.quantity} pièce(s)</Text></TouchableOpacity>} ListEmptyComponent={<Text className="p-8 text-center" style={{color:colors.textSecondary}}>Aucune commande reçue.</Text>}/>}</SafeScreen>;}
