import { ActivityIndicator, ScrollView, Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useMyArtisan } from '@/features/artisans/artisans.hooks';
export default function ArtisanProfileDashboard(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const profile=useMyArtisan();if(profile.isLoading)return <SafeScreen><View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary}/></View></SafeScreen>;if(profile.isError||!profile.data)return <SafeScreen><View className="flex-1 items-center justify-center px-8"><Text className="text-center" style={{color:colors.textSecondary}}>Votre profil artisan n’est pas encore configuré.</Text><TouchableOpacity onPress={()=>router.push('/(profile)/become-artisan')} className="mt-4"><Text className="font-bold text-[#EF4444]">Démarrer l’onboarding</Text></TouchableOpacity></View></SafeScreen>;const a=profile.data;return <SafeScreen><ScrollView contentContainerStyle={{padding:20}}><TouchableOpacity onPress={()=>router.back()}><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="mt-6 text-3xl font-extrabold" style={{color:colors.text}}>{a.displayName}</Text><Text className="mt-2 text-sm" style={{color:colors.textSecondary}}>{a.verificationStatus} · {a.countryCode}</Text><Text className="mt-6 text-base leading-7" style={{color:colors.textSecondary}}>{a.story}</Text><View className="mt-7 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><Text className="font-bold" style={{color:colors.text}}>Spécialités</Text><Text className="mt-2" style={{color:colors.textSecondary}}>{a.specialties.map((item)=>item.name).join(' · ')}</Text></View></ScrollView></SafeScreen>;}
