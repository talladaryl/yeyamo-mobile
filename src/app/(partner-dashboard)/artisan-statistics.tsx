import { Text, View } from 'react-native'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store';
export default function ArtisanStatistics(){const colors=useThemeStore((s)=>s.colors);return <SafeScreen><View className="flex-1 items-center justify-center px-8"><Icon name="stats-chart-outline" size={48} color={colors.textMuted}/><Text className="mt-4 text-center text-lg font-bold" style={{color:colors.text}}>Statistiques artisan</Text><Text className="mt-2 text-center" style={{color:colors.textSecondary}}>Les vues, favoris et revenus artisan seront affichés dès que l’endpoint analytics dédié sera disponible. Aucun chiffre fictif n’est injecté.</Text></View></SafeScreen>;}
