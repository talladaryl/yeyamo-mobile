import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useMyArtisan, useArtisanArtworks } from '@/features/artisans/artisans.hooks'; import { ArtworkCard } from '@/features/artworks/components/ArtworkCard';
export default function PartnerArtworksScreen(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const artisan=useMyArtisan();const artworks=useArtisanArtworks(artisan.data?.partnerId);return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 text-2xl font-extrabold" style={{color:colors.text}}>Mes œuvres</Text></View>{artisan.isLoading||artworks.isLoading?<View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary}/></View>:artisan.isError||artworks.isError?<State text="Le catalogue artisan est indisponible." retry={()=>{void artisan.refetch();void artworks.refetch();}}/>:<FlatList data={artworks.data?.content??[]} keyExtractor={(item)=>item.assetId} contentContainerStyle={{padding:16}} renderItem={({item})=><View className="mb-3"><ArtworkCard artwork={item} onPress={()=>router.push(`/(partner-dashboard)/artworks/${item.assetId}`)}/></View>} ListEmptyComponent={<State text="Aucune œuvre publiée."/>}/>}</SafeScreen>;}
function State({text,retry}:{text:string;retry?:()=>void}){const colors=useThemeStore((s)=>s.colors);return <View className="flex-1 items-center justify-center px-8"><Icon name="color-palette-outline" size={42} color={colors.textMuted}/><Text className="mt-4 text-center" style={{color:colors.textSecondary}}>{text}</Text>{retry?<TouchableOpacity onPress={retry} className="mt-4"><Text className="font-bold text-[#EF4444]">Réessayer</Text></TouchableOpacity>:null}</View>;}
