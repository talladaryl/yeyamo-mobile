import { ActivityIndicator, ScrollView, Text, TouchableOpacity, View } from 'react-native'; import { useLocalSearchParams, useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useArtwork, useArtworkOffer } from '@/features/artworks/artworks.hooks';
export default function PartnerArtworkDetail(){const {id}=useLocalSearchParams<{id:string}>();const router=useRouter();const colors=useThemeStore((s)=>s.colors);const detail=useArtwork(id);const offer=useArtworkOffer(id);if(detail.isLoading)return <SafeScreen><View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary}/></View></SafeScreen>;if(!detail.data)return <SafeScreen><View className="flex-1 items-center justify-center"><Text style={{color:colors.text}}>Œuvre indisponible.</Text></View></SafeScreen>;const a=detail.data.artwork;return <SafeScreen><ScrollView contentContainerStyle={{padding:20}}><TouchableOpacity onPress={()=>router.back()}><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="mt-6 text-3xl font-extrabold" style={{color:colors.text}}>{a.title}</Text><Text className="mt-2 text-sm" style={{color:colors.textSecondary}}>{a.availabilityStatus} · {a.authenticityStatus}</Text><Text className="mt-6 text-base leading-7" style={{color:colors.textSecondary}}>{a.story}</Text><View className="mt-7 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><Text className="font-bold" style={{color:colors.text}}>Offre</Text><Text className="mt-2 text-sm" style={{color:colors.textSecondary}}>{offer.data?`${offer.data.saleType} · ${offer.data.status}`:'Aucune offre active'}</Text></View></ScrollView></SafeScreen>;}
