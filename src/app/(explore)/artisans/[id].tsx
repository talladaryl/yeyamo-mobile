import { ActivityIndicator, ScrollView, Text, TouchableOpacity, View } from 'react-native';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { SafeScreen } from '@/components/ui/SafeScreen';
import { Icon } from '@/components/ui/Icon';
import { useThemeStore } from '@/features/theme/theme.store';
import { ArtworkCard } from '@/features/artworks/components/ArtworkCard';
import { useArtisan, useArtisanArtworks } from '@/features/artisans/artisans.hooks';
export default function ArtisanDetailScreen() { const { id } = useLocalSearchParams<{ id: string }>(); const router = useRouter(); const colors = useThemeStore((state) => state.colors); const artisan = useArtisan(id); const artworks = useArtisanArtworks(id); if (artisan.isLoading) return <SafeScreen><View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary} /></View></SafeScreen>; if (artisan.isError || !artisan.data) return <SafeScreen><View className="flex-1 items-center justify-center"><Text style={{ color: colors.text }}>Ce profil artisan est indisponible.</Text></View></SafeScreen>; const data = artisan.data; return <SafeScreen><ScrollView contentContainerStyle={{ paddingBottom: 32 }}><View className="flex-row items-center px-4 pt-3"><TouchableOpacity onPress={() => router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text} /></TouchableOpacity><View className="ml-2 flex-1" /><Icon name="share-outline" size={22} color={colors.text} /></View><View className="items-center px-5 pt-5"><View className="h-24 w-24 items-center justify-center rounded-full bg-[#FEE2E2]"><Icon name="person-outline" size={42} color="#EF4444" /></View><View className="mt-4 flex-row items-center"><Text className="text-2xl font-extrabold" style={{ color: colors.text }}>{data.displayName}</Text>{data.verificationStatus === 'VERIFIED' ? <Icon name="checkmark-circle" size={20} color="#22C55E" /> : null}</View><Text className="mt-1 text-sm" style={{ color: colors.textSecondary }}>{data.cityId ?? data.countryCode} · {data.specialties.map((specialty) => specialty.name).join(', ')}</Text><Text className="mt-5 self-stretch text-base leading-7" style={{ color: colors.textSecondary }}>{data.story}</Text></View><View className="mx-5 mt-6 rounded-2xl border p-4" style={{ borderColor: colors.border, backgroundColor: colors.card }}><Text className="font-bold" style={{ color: colors.text }}>Atelier et commandes</Text><Text className="mt-2 text-sm" style={{ color: colors.textSecondary }}>{data.craftDescription}</Text><Text className="mt-3 text-xs" style={{ color: colors.textMuted }}>{data.acceptsCustomOrders ? 'Commandes personnalisées acceptées' : 'Commandes personnalisées indisponibles'}{data.internationalShipping ? ' · Livraison internationale' : ''}</Text></View><View className="mt-7"><Text className="mb-3 px-5 text-lg font-bold" style={{ color: colors.text }}>Œuvres de l’artisan</Text>{artworks.isLoading ? <ActivityIndicator color={colors.primary} /> : <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ paddingHorizontal: 20 }}>{artworks.data?.content.map((artwork) => <ArtworkCard key={artwork.assetId} artwork={artwork} onPress={() => router.push(`/(explore)/artworks/${artwork.assetId}`)} />)}</ScrollView>}</View><View className="mx-5 mt-8"><TouchableOpacity onPress={() => router.push('/(tabs)/chats')} className="items-center rounded-xl bg-[#EF4444] px-4 py-4"><Text className="font-bold text-white">Contacter l’artisan</Text></TouchableOpacity><Text className="mt-3 text-center text-xs" style={{ color: colors.textMuted }}>L’abonnement artisan attend un endpoint public dédié ; il n’est pas simulé.</Text></View></ScrollView></SafeScreen>; }
