import { FlatList, Text, TouchableOpacity, View } from 'react-native'; import { Image } from 'expo-image'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { demoArtCategories } from '@/features/culture/culturalCatalog.demo';
export default function ArtCategories(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="h-11 w-11 items-center justify-center"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><View className="ml-2"><Text className="text-2xl font-extrabold" style={{color:colors.text}}>Arts du Cameroun</Text><Text className="text-xs" style={{color:colors.textSecondary}}>Explorez par savoir-faire.</Text></View></View><FlatList data={demoArtCategories} numColumns={2} keyExtractor={(item)=>item.id} columnWrapperStyle={{gap:12}} contentContainerStyle={{padding:16,paddingBottom:36}} renderItem={({item})=><TouchableOpacity onPress={()=>router.push(`/(explore)/art-categories/${item.id}`)} className="mb-3 flex-1 overflow-hidden rounded-2xl border" style={{backgroundColor:colors.card,borderColor:colors.border}}><Image source={{uri:item.imageUrl}} style={{width:'100%',height:125}} contentFit="cover"/><View className="p-3"><View className="flex-row items-center"><Icon name={item.icon} size={19} color={colors.primary}/><Text className="ml-2 flex-1 font-extrabold" style={{color:colors.text}}>{item.name}</Text></View><Text className="mt-2 text-xs leading-4" style={{color:colors.textSecondary}} numberOfLines={3}>{item.description}</Text></View></TouchableOpacity>}/></SafeScreen>}
