import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native'; import { useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { useArtworks } from '@/features/artworks/artworks.hooks'; import { demoArtCategories } from '@/features/culture/culturalCatalog.demo'; import { useAuthStore } from '@/features/auth/auth.store';
export default function ArtCategories(){const router=useRouter();const colors=useThemeStore((s)=>s.colors);const demo=useAuthStore((s)=>s.sessionMode?.startsWith('demo-')??false);const artworks=useArtworks({size:100});const categories=demo?demoArtCategories:(artworks.data?.content??[]).map((item)=>item.category).filter((value,index,all)=>Boolean(value)&&all.indexOf(value)===index).map((value)=>({id:value!,name:value!,description:'',icon:'color-palette-outline'}));return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="h-11 w-11 items-center justify-center"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 text-2xl font-extrabold" style={{color:colors.text}}>Arts</Text></View>{!demo&&artworks.isLoading?<ActivityIndicator className="mt-16" color={colors.primary}/>:<FlatList data={categories} numColumns={2} keyExtractor={(item)=>item.id} columnWrapperStyle={{gap:12}} contentContainerStyle={{padding:16,paddingBottom:36}} renderItem={({item})=><TouchableOpacity onPress={()=>router.push(`/(explore)/art-categories/${item.id}`)} className="mb-3 flex-1 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><Icon name="color-palette-outline" size={26} color={colors.primary}/><Text className="mt-3 font-extrabold" style={{color:colors.text}}>{item.name}</Text>{item.description?<Text className="mt-2 text-xs" style={{color:colors.textSecondary}}>{item.description}</Text>:null}</TouchableOpacity>} ListEmptyComponent={<Text className="p-8 text-center" style={{color:colors.textSecondary}}>Aucune catégorie publiée.</Text>}/>}</SafeScreen>}
