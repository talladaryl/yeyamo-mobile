import { ScrollView, Text, TouchableOpacity, View } from 'react-native'; import { Image } from 'expo-image'; import { useLocalSearchParams, useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { demoRecipes } from '@/features/culture/culturalCatalog.demo';
export default function RecipeDetail(){const {id}=useLocalSearchParams<{id:string}>();const router=useRouter();const colors=useThemeStore((s)=>s.colors);const item=demoRecipes.find((value)=>value.id===id);if(!item)return <SafeScreen><View className="flex-1 items-center justify-center"><Text style={{color:colors.text}}>Recette introuvable.</Text></View></SafeScreen>;return <SafeScreen><ScrollView contentContainerStyle={{paddingBottom:40}}><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="h-11 w-11 items-center justify-center"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 flex-1 text-lg font-extrabold" style={{color:colors.text}}>Recette</Text></View><Image source={{uri:item.imageUrl}} style={{width:'100%',height:260}} contentFit="cover"/><View className="px-5 pt-6"><Text className="text-xs font-extrabold text-[#EF4444]">{item.category.toUpperCase()} · {item.region.toUpperCase()}</Text><Text className="mt-2 text-3xl font-extrabold" style={{color:colors.text}}>{item.name}</Text><Text className="mt-3 leading-6" style={{color:colors.textSecondary}}>{item.description}</Text><View className="mt-5 flex-row gap-3"><Metric icon="time-outline" label={`${item.preparationMinutes} min`}/><Metric icon="speedometer-outline" label={item.difficulty}/></View><Text className="mb-3 mt-7 text-xl font-extrabold" style={{color:colors.text}}>Ingrédients</Text>{item.ingredients.map((ingredient)=><View key={ingredient} className="mb-2 flex-row items-center rounded-xl border p-3" style={{backgroundColor:colors.card,borderColor:colors.border}}><Icon name="checkmark-circle" size={18} color={colors.primary}/><Text className="ml-3" style={{color:colors.text}}>{ingredient}</Text></View>)}<Text className="mb-3 mt-7 text-xl font-extrabold" style={{color:colors.text}}>Préparation</Text>{item.steps.map((step,index)=><View key={step} className="mb-3 flex-row"><View className="h-8 w-8 items-center justify-center rounded-full bg-[#EF4444]"><Text className="font-bold text-white">{index+1}</Text></View><Text className="ml-3 flex-1 leading-6" style={{color:colors.textSecondary}}>{step}</Text></View>)}<Text className="mt-5 text-xs" style={{color:colors.textMuted}}>Recette mock destinée aux tests d’interface. Les quantités et pratiques culinaires doivent être vérifiées avant usage réel.</Text></View></ScrollView></SafeScreen>}
function Metric({icon,label}:{icon:string;label:string}){const colors=useThemeStore((s)=>s.colors);return <View className="flex-1 flex-row items-center rounded-xl border p-3" style={{backgroundColor:colors.card,borderColor:colors.border}}><Icon name={icon} size={19} color={colors.primary}/><Text className="ml-2 font-semibold" style={{color:colors.text}}>{label}</Text></View>}
