import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { SafeScreen } from '@/components/ui/SafeScreen';
import { Icon } from '@/components/ui/Icon';
import { useThemeStore } from '@/features/theme/theme.store';
import { useLanguageLessons } from '@/features/culture/culture.hooks';

export default function LanguageLessonsScreen() { const { code } = useLocalSearchParams<{ code: string }>(); const router = useRouter(); const colors = useThemeStore((state) => state.colors); const lessons = useLanguageLessons(code); return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={() => router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text} /></TouchableOpacity><Text className="ml-2 text-xl font-extrabold" style={{ color: colors.text }}>Leçons</Text></View>{lessons.isLoading ? <View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary} /></View> : <FlatList data={lessons.data ?? []} keyExtractor={(item) => item.id} contentContainerStyle={{ padding: 16, paddingBottom: 32 }} renderItem={({ item, index }) => <TouchableOpacity onPress={() => router.push(`/(explore)/language-lessons/${item.id}`)} className="mb-3 flex-row rounded-2xl border p-4" style={{ backgroundColor: colors.card, borderColor: colors.border }}><View className="h-10 w-10 items-center justify-center rounded-xl bg-[#FEE2E2]"><Text className="font-extrabold text-[#B91C1C]">{index + 1}</Text></View><View className="ml-3 flex-1"><Text className="font-bold" style={{ color: colors.text }}>{item.title}</Text><Text className="mt-1 text-sm" style={{ color: colors.textSecondary }} numberOfLines={2}>{item.description}</Text><Text className="mt-2 text-xs font-semibold" style={{ color: colors.textMuted }}>{item.estimatedMinutes} min · Niveau {item.difficulty}</Text></View><Icon name="chevron-forward" size={20} color={colors.textSecondary} /></TouchableOpacity>} ListEmptyComponent={<View className="items-center py-16"><Text style={{ color: colors.textSecondary }}>Aucune leçon disponible.</Text></View>} />}</SafeScreen>; }
