import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { ArtworkCard } from '@/features/artworks/components/ArtworkCard'; import { useArtworks } from '@/features/artworks/artworks.hooks'; import { useThemeStore } from '@/features/theme/theme.store'; import { demoArtCategories } from '@/features/culture/culturalCatalog.demo';
export default function ArtCategoryDetail(){const {slug}=useLocalSearchParams<{slug:string}>();const router=useRouter();const colors=useThemeStore((s)=>s.colors);const category=demoArtCategories.find((item)=>item.id===slug);const artworks=useArtworks({size:40});const filtered=(artworks.data?.content??[]).filter((item)=>item.category===slug);return <SafeScreen><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="h-11 w-11 items-center justify-center"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><View className="ml-2 flex-1"><Text className="text-2xl font-extrabold" style={{color:colors.text}}>{category?.name??'Catégorie artistique'}</Text><Text className="text-xs" style={{color:colors.textSecondary}}>{category?.description}</Text></View></View>{artworks.isLoading?<ActivityIndicator className="mt-16" color={colors.primary}/>:<FlatList data={filtered} keyExtractor={(item)=>item.assetId} contentContainerStyle={{padding:16,paddingBottom:36}} renderItem={({item})=><View className="mb-3"><ArtworkCard artwork={item} onPress={()=>router.push(`/(explore)/artworks/${item.assetId}`)}/></View>} ListEmptyComponent={<Text className="p-8 text-center" style={{color:colors.textSecondary}}>Aucune œuvre dans cette catégorie.</Text>}/>}</SafeScreen>}
