import { Share, ScrollView, Text, TouchableOpacity, View } from 'react-native';
import { Image } from 'expo-image';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { demoProverbs } from '@/features/culture/culturalCatalog.demo';
export default function ProverbDetail(){const {id}=useLocalSearchParams<{id:string}>();const router=useRouter();const colors=useThemeStore((s)=>s.colors);const item=demoProverbs.find((value)=>value.id===id);if(!item)return <SafeScreen><View className="flex-1 items-center justify-center"><Text style={{color:colors.text}}>Proverbe introuvable.</Text></View></SafeScreen>;return <SafeScreen><ScrollView contentContainerStyle={{paddingBottom:40}}><View className="flex-row items-center px-4 py-3"><TouchableOpacity onPress={()=>router.back()} className="h-11 w-11 items-center justify-center"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 flex-1 text-lg font-extrabold" style={{color:colors.text}}>Proverbe</Text><TouchableOpacity onPress={()=>void Share.share({message:`${item.text}\n\n${item.meaning}`})} className="h-11 w-11 items-center justify-center"><Icon name="share-outline" size={22} color={colors.text}/></TouchableOpacity></View><Image source={{uri:item.imageUrl}} style={{width:'100%',height:240}} contentFit="cover"/><View className="px-5 pt-6"><View className="self-start rounded-full px-3 py-1.5" style={{backgroundColor:colors.accentSoft}}><Text className="text-xs font-bold" style={{color:colors.primary}}>{item.category} · {item.region}</Text></View><Text className="mt-5 text-3xl font-extrabold leading-10" style={{color:colors.text}}>« {item.text} »</Text><Text className="mt-2 text-sm" style={{color:colors.textSecondary}}>Langue de transmission : {item.language.toUpperCase()}</Text><Block title="Signification" text={item.meaning}/><Block title="Quand l’utiliser ?" text={item.usage}/><Block title="Traduction française" text={item.translation}/><Text className="mt-7 text-xs" style={{color:colors.textMuted}}>Donnée de démonstration à faire valider par les communautés et spécialistes concernés avant publication éditoriale.</Text></View></ScrollView></SafeScreen>}
function Block({title,text}:{title:string;text:string}){const colors=useThemeStore((s)=>s.colors);return <View className="mt-6 rounded-2xl border p-4" style={{backgroundColor:colors.card,borderColor:colors.border}}><Text className="font-extrabold" style={{color:colors.text}}>{title}</Text><Text className="mt-2 leading-6" style={{color:colors.textSecondary}}>{text}</Text></View>}
