import { ActivityIndicator, ScrollView, Text, TouchableOpacity, View } from 'react-native'; import { useLocalSearchParams, useRouter } from 'expo-router'; import { SafeScreen } from '@/components/ui/SafeScreen'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import { preferredTranslation } from '@/features/culture/culture.mappers'; import { useCultureContent, useCultureGraphRelations } from '@/features/culture/culture.hooks';
export default function CultureDetailScreen(){const {id}=useLocalSearchParams<{id:string}>();const router=useRouter();const colors=useThemeStore((s)=>s.colors);const detail=useCultureContent(id);const related=useCultureGraphRelations(id);if(detail.isLoading)return <SafeScreen><View className="flex-1 items-center justify-center"><ActivityIndicator color={colors.primary}/></View></SafeScreen>;if(detail.isError||!detail.data)return <SafeScreen><View className="flex-1 items-center justify-center px-6"><Icon name="alert-circle-outline" size={36} color={colors.primary}/><Text className="mt-4 text-center font-bold" style={{color:colors.text}}>Ce contenu n’est pas accessible.</Text><TouchableOpacity onPress={()=>detail.refetch()} className="mt-4"><Text className="font-bold text-[#EF4444]">Réessayer</Text></TouchableOpacity></View></SafeScreen>;const {content,translations}=detail.data;const translation=preferredTranslation(translations);const sensitive=content.sensitivityLevel!=='PUBLIC';return <SafeScreen><ScrollView contentContainerStyle={{paddingBottom:32}}><View className="flex-row items-center px-4 pt-3"><TouchableOpacity onPress={()=>router.back()} className="-ml-2 p-2"><Icon name="chevron-back" size={24} color={colors.text}/></TouchableOpacity><Text className="ml-2 flex-1 text-lg font-bold" style={{color:colors.text}}>Contenu culturel</Text><Icon name="bookmark-outline" size={22} color={colors.text}/></View>{sensitive?<View className="mx-4 mt-4 rounded-xl bg-[#FEF3C7] p-3"><Text className="font-bold text-[#92400E]">Contenu à consulter avec respect</Text><Text className="mt-1 text-sm text-[#92400E]">Les règles et restrictions publiées par la communauté s’appliquent.</Text></View>:null}<View className="px-5 pt-7"><Text className="text-xs font-bold text-[#B91C1C]">{content.type.replace(/_/g,' ')}</Text><Text className="mt-2 text-3xl font-extrabold" style={{color:colors.text}}>{translation?.title??content.slug.replace(/-/g,' ')}</Text><Text className="mt-3 text-sm" style={{color:colors.textSecondary}}>{content.countryCode} · {content.primaryLanguageCode.toUpperCase()}{content.communityName?` · ${content.communityName}`:''}</Text><View className="mt-6 border-t pt-5" style={{borderColor:colors.border}}><Text className="text-base leading-7" style={{color:colors.text}}>{translation?.body??translation?.summary??'La traduction détaillée sera disponible dès sa publication.'}</Text></View>{translation?.summary?<View className="mt-5 rounded-xl p-4" style={{backgroundColor:colors.elevated}}><Text className="font-semibold" style={{color:colors.text}}>{translation.summary}</Text></View>:null}{related.data?.length?<View className="mt-8"><Text className="text-lg font-bold" style={{color:colors.text}}>À découvrir aussi</Text>{related.data.slice(0,6).map((item)=><View key={`${item.targetId}-${item.relationType}`} className="mt-2 rounded-xl border p-3" style={{borderColor:colors.border}}><Text className="font-semibold" style={{color:colors.text}}>{item.targetLabel}</Text><Text className="mt-1 text-xs" style={{color:colors.textMuted}}>{item.relationType}</Text></View>)}</View>:null}<View className="mt-7 flex-row gap-3"><Action icon="share-outline" label="Partager"/><Action icon="flag-outline" label="Signaler"/></View><Text className="mt-8 text-xs" style={{color:colors.textMuted}}>Source : {content.sourceType} · Vérification : {content.verificationStatus}</Text></View></ScrollView></SafeScreen>;}
function Action({icon,label}:{icon:string;label:string}){const colors=useThemeStore((s)=>s.colors);return <TouchableOpacity className="flex-row items-center rounded-xl border px-4 py-3" style={{borderColor:colors.border}}><Icon name={icon} size={19} color={colors.text}/><Text className="ml-2 font-semibold" style={{color:colors.text}}>{label}</Text></TouchableOpacity>;}
