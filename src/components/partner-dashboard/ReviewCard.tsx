import { Text, TouchableOpacity, View } from 'react-native';
import { Avatar } from '@/components/ui/Avatar'; import { Icon } from '@/components/ui/Icon'; import type { CustomerReview } from '@/features/partner-dashboard/types'; import { useThemeStore } from '@/features/theme/theme.store';
export function ReviewCard({ review, onReply }: { review: CustomerReview; onReply: () => void }) { const colors = useThemeStore((state) => state.colors); return <View className="mb-3 rounded-2xl border p-4" style={{ backgroundColor: colors.card, borderColor: colors.border }}><View className="mb-3 flex-row items-start"><Avatar uri={review.customer_avatar} displayName={review.customer_name} size={42} /><View className="ml-3 flex-1"><Text className="font-bold" style={{ color: colors.text }}>{review.customer_name}</Text><View className="mt-1 flex-row items-center">{Array.from({ length: 5 }).map((_, index) => <Icon key={index} name={index < review.rating ? 'star' : 'star-outline'} size={13} color={index < review.rating ? '#F59E0B' : colors.textMuted} />)}<Text className="ml-2 text-xs" style={{ color: colors.textMuted }}>{review.date}</Text></View></View></View><Text className="text-sm leading-5" style={{ color: colors.text }}>{review.comment}</Text>{review.partner_reply ? <View className="mt-4 rounded-2xl border-l-4 p-3" style={{ backgroundColor: colors.elevated, borderColor: colors.primary }}><Text className="text-xs font-extrabold" style={{ color: colors.primary }}>Votre réponse</Text><Text className="mt-1 text-sm leading-5" style={{ color: colors.textSecondary }}>{review.partner_reply}</Text></View> : null}<View className="mt-4 flex-row items-center border-t pt-3" style={{ borderColor: colors.border }}><Text className="flex-1 text-xs" style={{ color: colors.textSecondary }}>{review.establishment}</Text><TouchableOpacity onPress={onReply} className="rounded-xl px-3 py-2" style={{ backgroundColor: colors.accentSoft }} accessibilityLabel={`Répondre à ${review.customer_name}`}><Text className="text-xs font-bold" style={{ color: colors.primary }}>{review.partner_reply ? 'Modifier la réponse' : 'Répondre'}</Text></TouchableOpacity></View></View>; }
