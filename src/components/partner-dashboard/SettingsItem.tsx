import { Text, TouchableOpacity, View } from 'react-native';
import { Icon } from '@/components/ui/Icon'; import type { SettingsItem as SettingsItemType } from '@/features/partner-dashboard/types'; import { useThemeStore } from '@/features/theme/theme.store';
export function SettingsItem({ item, onPress }: { item: SettingsItemType; onPress: () => void }) { const colors = useThemeStore((state) => state.colors); return <TouchableOpacity onPress={onPress} className="mb-2 min-h-14 flex-row items-center rounded-2xl border px-4 py-3" style={{ backgroundColor: colors.card, borderColor: colors.border }} activeOpacity={0.8}><View className="h-10 w-10 items-center justify-center rounded-xl" style={{ backgroundColor: colors.elevated }}><Icon name={item.icon} size={21} color={colors.textSecondary} /></View><Text className="ml-3 flex-1 text-sm font-semibold" style={{ color: colors.text }}>{item.label}</Text>{item.value ? <Text className="mr-2 text-sm" style={{ color: colors.textSecondary }}>{item.value}</Text> : null}{item.hasArrow ? <Icon name="chevron-forward" size={18} color={colors.textMuted} /> : null}</TouchableOpacity>; }
