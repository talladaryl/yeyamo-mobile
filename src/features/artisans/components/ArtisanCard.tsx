import { Text, TouchableOpacity, View } from 'react-native'; import { Image } from 'expo-image'; import { Icon } from '@/components/ui/Icon'; import { useThemeStore } from '@/features/theme/theme.store'; import type { Artisan } from '../artisans.types';
export function ArtisanCard({ artisan, onPress }: { artisan: Artisan; onPress: () => void }) { const colors = useThemeStore((state) => state.colors); return <TouchableOpacity onPress={onPress} className="mr-3 w-64 overflow-hidden rounded-2xl border" style={{ backgroundColor: colors.card, borderColor: colors.border }} activeOpacity={0.86}><View className="h-32" style={{ backgroundColor: colors.elevated }}>{artisan.coverImageUrl ? <Image source={{ uri: artisan.coverImageUrl }} style={{ width: '100%', height: '100%' }} contentFit="cover" /> : null}<View className="absolute -bottom-7 left-4 rounded-full border-4" style={{ borderColor: colors.card }}>{artisan.avatarUrl ? <Image source={{ uri: artisan.avatarUrl }} style={{ width: 54, height: 54, borderRadius: 27 }} contentFit="cover" /> : <View className="h-[54px] w-[54px] items-center justify-center rounded-full" style={{ backgroundColor: colors.elevated }}><Icon name="person-outline" size={23} color={colors.textSecondary} /></View>}</View></View><View className="p-4 pt-9"><View className="flex-row items-center"><Text className="flex-1 font-extrabold" style={{ color: colors.text }} numberOfLines={1}>{artisan.displayName}</Text>{artisan.verificationStatus === 'VERIFIED' ? <Icon name="checkmark-circle" size={17} color="#1689FF" /> : null}</View><Text className="mt-1 text-sm leading-5" style={{ color: colors.textSecondary }} numberOfLines={2}>{artisan.craftDescription}</Text><Text className="mt-3 text-xs font-semibold" style={{ color: colors.textMuted }}>{artisan.cityId} · {artisan.specialties.map((item) => item.name).join(', ')}</Text></View></TouchableOpacity>; }
