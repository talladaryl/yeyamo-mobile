import { Text, TouchableOpacity, View } from 'react-native';
import { Icon } from '@/components/ui/Icon';
import { useThemeStore } from '@/features/theme/theme.store';
import type { Artisan } from '../artisans.types';
export function ArtisanCard({ artisan, onPress }: { artisan: Artisan; onPress: () => void }) { const colors = useThemeStore((state) => state.colors); return <TouchableOpacity onPress={onPress} className="mr-3 w-64 rounded-2xl border p-4" style={{ backgroundColor: colors.card, borderColor: colors.border }}><View className="h-11 w-11 items-center justify-center rounded-full bg-[#FEE2E2]"><Icon name="person-outline" size={22} color="#EF4444" /></View><View className="mt-4 flex-row items-center"><Text className="flex-1 font-bold" style={{ color: colors.text }} numberOfLines={1}>{artisan.displayName}</Text>{artisan.verificationStatus === 'VERIFIED' ? <Icon name="checkmark-circle" size={17} color="#22C55E" /> : null}</View><Text className="mt-1 text-sm" style={{ color: colors.textSecondary }} numberOfLines={2}>{artisan.craftDescription}</Text><Text className="mt-3 text-xs font-semibold" style={{ color: colors.textMuted }}>{artisan.cityId ?? artisan.countryCode} · {artisan.specialties.map((item) => item.name).join(', ')}</Text></TouchableOpacity>; }
