import { useCallback, useState } from 'react';
import * as Location from 'expo-location';
export interface CurrentLocation{latitude:number;longitude:number;accuracy:number|null}
export function useLocation(){const[location,setLocation]=useState<CurrentLocation|null>(null);const[isLoading,setIsLoading]=useState(false);const[error,setError]=useState<string|null>(null);const[permissionStatus,setPermissionStatus]=useState<Location.PermissionStatus|null>(null);const requestLocation=useCallback(async()=>{setIsLoading(true);setError(null);try{let permission=await Location.getForegroundPermissionsAsync();if(!permission.canAskAgain&&permission.status!==Location.PermissionStatus.GRANTED){setPermissionStatus(permission.status);setError('Autorisation de localisation refusée. Activez-la dans les réglages.');return null;}if(permission.status!==Location.PermissionStatus.GRANTED)permission=await Location.requestForegroundPermissionsAsync();setPermissionStatus(permission.status);if(permission.status!==Location.PermissionStatus.GRANTED){setError('Autorisation de localisation refusée.');return null;}const current=await Promise.race([Location.getCurrentPositionAsync({accuracy:Location.Accuracy.Balanced}),new Promise<never>((_,reject)=>setTimeout(()=>reject(new Error('Location timeout')),12_000))]);const result={latitude:current.coords.latitude,longitude:current.coords.longitude,accuracy:current.coords.accuracy};setLocation(result);return result;}catch{setError('Position indisponible. Réessayez dans quelques instants.');return null;}finally{setIsLoading(false);}},[]);return{location,isLoading,loading:isLoading,error,permissionStatus,requestLocation,refresh:requestLocation};}
